#* Common variables for all archetypes *#
#set($ModuleName = "$artifactId")
#set($ModuleName = $ModuleName.replaceAll("[-_!@#$%^&*()+:; ]", ""))
#set($ModuleName = "${ModuleName.substring(0,1).toUpperCase()}${ModuleName.substring(1)}")
#set($ModuleNameLower = "${ModuleName.toLowerCase()}")

#* Setting specifict to this archetype *#
##set($artifactId = "${ModuleNameLower}")
##set($ModuleType = "WebApp")

#set($package = $groupId.replaceAll("[.]", "/"))

WELCOME
=======
Congratulations you have just created a new iBeans webapp!

You can quickly test your application by running:

mvn -f tcat.xml

from the root directory and in your browser goto http://localhost:8888

ADDITIONAL RESOURCES
====================
TODO Add doc links

The Mule iBeans Team

--------------------------------------------------------------------
This project was auto-generated by the ibeans-webapp-archetype.

artifactId=${artifactId}
description=${description}
ibeansVersion=${ibeansVersion}

version=${project.version}
groupId=${groupId}
basedir=${basedir}
--------------------------------------------------------------------